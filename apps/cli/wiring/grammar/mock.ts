/**
 * CLI Composition Root: Mock Grammar.
 */

import { createMockGrammar } from '@ksc/language-mock/grammar/index.js';

/** Singleton grammar instance for the mock grammar. */
export const grammar = createMockGrammar();
