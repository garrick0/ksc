{
  "name": "kindscript",
  "version": "0.1.0",
  "description": "Architectural enforcement for TypeScript through the type system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "ksc": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  }
}
