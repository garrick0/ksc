/**
 * ksc-behavior — KSC-specific equation functions and domain types.
 */

// Domain types
export type { KindDefinition, CheckerDiagnostic, AttributeDepGraph, PropertySet, Kind } from './types.js';

// Attribute type registry
export type { KSCAttrMap } from './attr-types.js';

// Equation context interface
export type { Ctx } from './ctx.js';
